{
  "name": "persona-census",
  "version": "1.0.0",
  "description": "Longest-running live Meta ads from tracked DTC competitors, tagged by persona and psychographic.",
  "scripts": {
    "harvest": "node scripts/harvest.mjs",
    "images": "node scripts/images.mjs",
    "build": "node scripts/build.mjs",
    "all": "node scripts/harvest.mjs && node scripts/images.mjs && node scripts/build.mjs",
    "serve": "node --run build && python3 -m http.server 8080 --directory public"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.56.1",
    "playwright-core": "^1.56.1",
    "sharp": "^0.35.4"
  },
  "private": true,
  "type": "module"
}
